{
  "name": "@ractix/example",
  "private": true,
  "version": "0.0.0",
  "description": "Example for Reactive UI",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rm -rf dist && NODE_ENV=production parcel build src/index.html --experimental-scope-hoisting --detailed-report"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.29.0"
  },
  "browserslist": [
    "since 2017-06"
  ],
  "dependencies": {
    "ractix": "^0.3.1",
    "@ractix/router": "^0.3.1"
  }
}
